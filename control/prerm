#!/bin/sh
set -e
# Only remove data on real uninstall, not on upgrade/reinstall
if [ "$1" = "remove" ]; then
    rm -f /etc/enigma2/openligadb_settings.json || true
    rm -f /etc/enigma2/openligadb_subscriptions.json || true
    rm -f /etc/enigma2/openligadb_state.json || true
fi
exit 0
